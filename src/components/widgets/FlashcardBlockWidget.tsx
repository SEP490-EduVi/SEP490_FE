'use client';

/**
 * FlashcardBlockWidget
 * ====================
 * Re-exports the interactive FlashcardBlock component as a widget.
 * Inserted into a slide via the Toolbar "Thẻ nhớ" button.
 */

export { FlashcardBlock as FlashcardBlockWidget } from '@/components/interactive';
